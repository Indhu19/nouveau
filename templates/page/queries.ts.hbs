import { queryOptions } from '@tanstack/react-query';

export const fetch{{pascalCase name}}Data = (): Promise<{ message: string }> => {
  return new Promise(resolve => {
    setTimeout(() => {
      resolve({ message: '{{pascalCase name}} loaded successfully!' });
    }, 1000);
  });
};

export const {{camelCase name}}QueryOptions = () =>
  queryOptions({
    queryKey: ['{{kebabCase name}}'],
    queryFn: fetch{{pascalCase name}}Data,
  });
